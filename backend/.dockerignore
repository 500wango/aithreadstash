# Backend Docker build context ignore list
# Purpose: reduce context size and avoid packaging secrets or dev-only files

# Node & package manager noise
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build output (will be produced in builder stage)
dist

# Environment files (never bake secrets into images)
.env
.env.*
!\.env.example

# Tests, coverage, reports
coverage
jest.config.js
jest_output.txt
jest_results.json
test/

# Editor/OS artifacts
.vscode
.idea
*.swp
*.swo
.DS_Store
Thumbs.db

# Linting/cache
.eslintcache

# Tooling and local scripts not needed for image build
scripts/

# Misc
.nyc_output